<div class="small">
  <span class="text-muted">
    <em>Acknowledgements</em>
    <span class="badge"><%= usernames.length %></span>
  </span>
  <% for (var i = usernames.length - 1; i >= 0; i--) { %>
    <strong><%- usernames[i] %></strong><% if (i > 0) { %>, <% } %>
  <% } %>
</div>